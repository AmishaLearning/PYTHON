#Modules
import Calculationmodule# using this we have to mention calculationmodule for using any of the function inside
from Calculationmodule import *# Using this no need to specify calculationmodule

a=6
b=7

# print(Calculationmodule.add(a,b)) 
print(add(a,b)) 
print(sub(a,b)) 